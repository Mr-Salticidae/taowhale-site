# 鲸海拾贝官网 · 服务器部署指南

目标服务器:43.128.2.110(假定 Ubuntu/Debian,root 或 sudo 权限)

## 0. 包内文件
```
鲸海拾贝官网-部署包/
├── backend/main.py            # FastAPI 后端(账号 + 论坛 + 静态托管)
├── backend/requirements.txt
├── static/index.html          # 前端(已接 API)
├── Dockerfile
├── docker-compose.yml
├── data/                      # SQLite 数据落盘目录(自动生成 whale.db)
└── 部署指南.md
```

## 1. 服务器安装 Docker(已装可跳过)
SSH 登录服务器后:
```bash
curl -fsSL https://get.docker.com | sh
systemctl enable --now docker
```
国内网络若拉取慢,可配置镜像加速(/etc/docker/daemon.json)。

## 2. 上传部署包
在你本地电脑(Windows PowerShell)执行:
```powershell
scp -r "E:\AIGC工作站\鲸海拾贝官网-部署包" root@43.128.2.110:/opt/whalesea
```
(或用 WinSCP / 宝塔面板上传到 /opt/whalesea)

## 3. 修改密钥并启动
SSH 到服务器:
```bash
cd /opt/whalesea
# 生成随机密钥并替换 compose 中的 JWT_SECRET
sed -i "s/CHANGE_ME_TO_RANDOM_64_CHARS/$(openssl rand -hex 32)/" docker-compose.yml
docker compose up -d --build
```

## 4. 开放 80 端口
- 云厂商控制台 → 安全组/防火墙 → 入站规则放行 TCP 80
- 若服务器开了 ufw:`ufw allow 80/tcp`

## 5. 验证
```bash
curl http://127.0.0.1/api/health     # 服务器本机
```
浏览器访问 **http://43.128.2.110** :
- 右上角注册一个账号 → 登录态出现头像菜单
- 社区论坛应显示「数据来源 LIVE」,可发帖、回帖
- 种子数据自带官方置顶帖《社区版规与发帖指南》

## 6. 日常运维
```bash
docker compose logs -f          # 看日志
docker compose restart          # 重启
docker compose up -d --build    # 更新代码后重建
cp data/whale.db /backup/       # 备份数据库(单文件)
```

## 7. 管理后台(V4.1 起)
- 官方账号:`admin@taowhale.local`(品牌更名前为 admin@whalesea.local,启动时自动迁移),密码由 compose 环境变量 `ADMIN_PASSWORD` 注入,每次容器启动会重置为该值。
- 品牌英文名为 **taowhale**(2026-06-11 起);服务器侧技术标识(容器名 whalesea、目录 /opt/whalesea、whale.db)保留旧名,不影响对外品牌展示。
- 登录官方账号后,右上角头像菜单出现「🛠 管理后台」(即 `#/admin`):数据概览、帖子置顶/删除、用户封禁/角色管理。
- 论坛帖子详情页内,官方账号可直接置顶/删帖/删回复。
- 被封禁用户:无法登录,已有 token 立即失效(接口返回 403)。
- 内容管理(V4.2 起):管理后台「内容管理」标签可录入/编辑/删除五类内容(作品/商单需求/合作案例/知识文档/课程),前台四板块实时读取;首次启动自动写入与旧版占位一致的种子数据,逐条替换为真实内容即可。

## 8. 已知边界(下一步可做)
- HTTP 明文:建议绑定域名后用 Caddy/Nginx + Let's Encrypt 上 HTTPS
- 短信/微信登录、作品/商单/知识库动态化、富文本与图片上传:均预留,按需迭代

## 9. 实际部署记录(2026-06-11,已上线)
与上文模板的差异,以下为服务器 43.128.2.110 的实际状态:

- **SSH 登录**:用户为 `ubuntu`(腾讯云镜像禁用 root 密码登录),免密密钥为本地 `~/.ssh/id_ed25519_whalesea`,`ubuntu` 可无密码 sudo。运维命令需加 `sudo` 执行。
- **Docker**:服务器已预装(29.5.2 + compose v5.1.4),未执行安装步骤。
- **端口布局**(80 原被既有 webupload 应用占用,经协调后调整):
  - 80 → nginx 反代鲸海拾贝官网(配置 `/etc/nginx/sites-available/whalesea`)
  - 127.0.0.1:8081 → whalesea 容器(compose 中 ports 已改为 `127.0.0.1:8081:8000`,仅本机可达)
  - 8001 → 既有 webupload 应用(原 80,已迁移;切换前备份在服务器 `/tmp/webupload.nginx.bak`)
  - 8080 → 既有 media 静态站;3000 → 既有 maieutic 容器(均未受影响)
- **JWT_SECRET**:已在服务器端替换为随机 64 位 hex,仅存在于服务器 `/opt/whalesea/docker-compose.yml`。
- **验证结果**:`/api/health` 服务器本机与外网(http://43.128.2.110/api/health)均返回 `{"ok":true}`,首页 200。
- **待办**:腾讯云安全组放行 TCP 8001(webupload 外网恢复访问);建议修改服务器密码或禁用 SSH 密码登录;HTTPS 待域名绑定后配置。
