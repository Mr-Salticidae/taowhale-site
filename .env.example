# 生产部署环境变量模板(供 docker-compose 使用)。
# 用法:cp .env.example .env 并填入真实值;.env 已被 .gitignore,绝不入库。
# docker compose 会自动读取同目录下的 .env 做变量替换。

# JWT 签名密钥 —— 必填,务必用随机长串。生成:openssl rand -hex 32
JWT_SECRET=

# 官方账号(admin@taowhale.local)登录密码 —— 必填,设置后每次启动会重置为该值
ADMIN_PASSWORD=
